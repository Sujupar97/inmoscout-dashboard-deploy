import React from 'react';

// This component is now obsolete. The scraping process is handled
// by the automated orchestrator, managed by SUPER_ADMINS in the
// new 'Orchestrator' view.
export const ScraperControlPanel: React.FC = () => {
  return null;
};