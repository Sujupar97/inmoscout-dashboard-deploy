import React from 'react';

// This component is now obsolete. Zone selection is defined centrally
// in `config.ts` for the automated orchestrator.
export const ZoneSelectionModal: React.FC = () => {
  return null;
};