import React from 'react';

// This component is now obsolete. Scraping is managed by the automated
// orchestrator view, which provides superior, persistent status tracking.
export const ScrapingProcessModal: React.FC = () => {
  return null;
};